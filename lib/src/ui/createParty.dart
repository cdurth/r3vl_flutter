import 'package:flutter/material.dart';

class CreateParty extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Create Party'),
        ),
        body: Center(child: Text('Create Party Placeholder'))
    );
  }
}